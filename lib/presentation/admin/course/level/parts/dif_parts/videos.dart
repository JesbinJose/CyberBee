import 'package:flutter/material.dart';

class PartVideoAddWidget extends StatelessWidget {
  const PartVideoAddWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}