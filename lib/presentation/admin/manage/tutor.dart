import 'package:flutter/material.dart';

class ManageTutor extends StatelessWidget {
  const ManageTutor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}