import 'package:flutter/material.dart';

class SinglePartScreen extends StatelessWidget {
  const SinglePartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}