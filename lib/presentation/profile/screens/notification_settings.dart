import 'package:flutter/material.dart';

class UserNotificationSettingsScreen extends StatelessWidget {
  const UserNotificationSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}